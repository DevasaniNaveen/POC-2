
pipeline {
    agent any

    stages {
        stage('Clone Code') {
            steps {
                sh 'echo passed'
                //git 'https://github.com/DevasaniNaveen/project-2-cicd.git'
            }
        }

        stage('Build & Test') {
            steps {
                echo 'Running build and test steps...'
            }
        }

        stage('Deploy with Ansible') {
            steps {
                ansiblePlaybook inventory: 'localhost',
                playbook: 'ansible/deploy.yml'
